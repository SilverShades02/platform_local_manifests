## Santoni Trees

To add to your local manifests, use a command like this:

```bash
git clone https://github.com/StrangeNoob/platform_local_manifests -b 9.0 .repo/local_manifests

```

Then to sync up:

```
repo sync --force-sync
```
